import {DocumentEnum} from './document.enum';

export class PersonModel {
  documentType: number;
  nid: string;
  firstName: string;
  lastName: string;
  birthdate: string;
  email: string;
  phone: string;
}
